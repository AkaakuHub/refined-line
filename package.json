{
  "name": "better-line",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "check:js": "biome check src-tauri/injections/**/*.js --fix",
    "check:rust": "cd src-tauri && cargo check",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@tauri-apps/cli": "^2",
    "lefthook": "^2.0.13"
  }
}
