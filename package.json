{
  "name": "refined-line",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "check:js": "biome format src-tauri/injections --write",
    "check:rust": "cd src-tauri && cargo check",
    "fmt:rust": "cd src-tauri && cargo fmt",
    "postinstall": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@tauri-apps/cli": "^2",
    "lefthook": "^2.0.13"
  }
}
